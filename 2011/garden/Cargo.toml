[package]
name = "garden"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"

[lib]
crate-type = ["staticlib"]

[dependencies]
delegate = "0.13.5"
mimalloc = "0.1.48"
